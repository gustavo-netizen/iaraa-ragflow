# DocMind 0.8 Python Dependencies
#
# System dependencies (install separately):
#   - poppler: brew install poppler (macOS) / apt install poppler-utils (Linux)
#   - Python >= 3.7
#
dashscope>=1.14.0
pdf2image>=1.16.0
Pillow>=9.0.0
PyYAML>=6.0
PyPDF2>=3.0.0
filelock>=3.0.0
psutil>=5.9.0
